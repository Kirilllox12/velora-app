{
  "name": "velora",
  "version": "1.0.0",
  "description": "Velora Messenger",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "build": "echo 'No build needed'",
    "cap:init": "npx cap init Velora com.velora.app --web-dir .",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "serve": "^14.0.0"
  }
}
